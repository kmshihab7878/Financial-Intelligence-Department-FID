name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: pip install ruff
      - name: Ruff lint
        run: ruff check src/ tests/unit/
      - name: Ruff format check
        run: ruff format --check src/ tests/unit/

  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: pip install -e ".[dev]" types-PyYAML
      - name: mypy
        run: mypy src/aiswarm/ --ignore-missing-imports

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: pip install -e ".[dev]"
      - name: Run tests
        run: pytest tests/unit/ --cov=src/aiswarm --cov-report=term-missing --cov-fail-under=60
        env:
          AIS_RISK_HMAC_SECRET: test-secret-for-ci
          AIS_API_KEY: test-api-key-for-ci

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: pip install pip-audit bandit
      - name: pip-audit
        run: pip-audit --strict --ignore-vuln PYSEC-2024-*
      - name: bandit
        run: bandit -r src/aiswarm/ -c pyproject.toml || bandit -r src/aiswarm/ -ll
      - name: Check for secrets
        run: |
          if grep -r "STAKE_API_KEY=.\{10,\}" .env 2>/dev/null; then
            echo "ERROR: Real API key found in .env"
            exit 1
          fi

  docker:
    runs-on: ubuntu-latest
    needs: [lint, typecheck, test]
    steps:
      - uses: actions/checkout@v4
      - name: Build Docker image
        run: docker build -t ais:test .
